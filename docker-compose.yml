version: '3.8'
services:
  catalogdb:
   image: mongo
  basketdb:
    image: redis
  identitydb:
   image: mcr.microsoft.com/mssql/server
  orderdb:
   image: mcr.microsoft.com/mssql/server
  rabbitmq:
   image: rabbitmq   
volumes:
 catalogdb_volume: 
 identitydb_volume:
 orderdb_volume:
